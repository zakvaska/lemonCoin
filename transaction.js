class Transaction {
    constructor(amount, day, cycleIndex, currency, type, source, sourceID, target, targetID) {
        this.properties = {
            amount: amount,
            day: day,
            cycleIndex: cycleIndex,
            tokenPrice: tokenPrice,
            currency: currency,
            type: type,
            source: source,
            sourceID: sourceID,
            target: target,
            targetID: targetID
        }
    }
}